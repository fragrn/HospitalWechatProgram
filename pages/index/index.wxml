<!--index.wxml
<view class="container">
  <view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
    <block wx:else>
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block>
  </view>
  <view class="usermotto">
    <text class="user-motto">{{motto}}</text>
  </view>
</view>
-->
  <view class="v1" style="height:{{clientHeight?clientHeight+'px':'auto'}}">
 

   <view class="v2">
   <view class="dltext" style="width: 232rpx; height: 92rpx; display: block; box-sizing: border-box; left: 0rpx; top: -2rpx">花狮校医</view>
   <view class="id_number">
       <input placeholder="请输入学号" type="number" bindinput="id_num" />
     </view>
    
     <view class=passwords">
     
       <input placeholder="请输入密码" type="password" bindinput="password" />
     </view>
  
     <view class="login_btn">
       <button class="btn-log" type="warn" bindtap="login">登录</button>
     </view>

  </view>
</view>
